# Raaed RAG Pipeline - Requirements
# ====================================
# For best results, use the conda environment.yml instead.
# If using pip directly, install in this exact order:
#
#   1. Install PyTorch first:     pip install torch torchvision torchaudio
#   2. Install PaddlePaddle:      pip install paddlepaddle
#   3. Pin critical deps:         pip install "numpy>=1.24,<2" "tokenizers>=0.19,<0.30"
#   4. Install everything else:   pip install -r requirements.txt
#
# Python >= 3.10 required.

# ── Web Framework ────────────────────────────────────────────────────
fastapi==0.110.2
uvicorn[standard]==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic-settings>=2.2.1,<3.0.0
aiofiles==23.2.1

# ── Database ─────────────────────────────────────────────────────────
# motor 3.4.0 is broken with pymongo>=4.9 (_QUERY_OPTIONS removed).
# motor>=3.6.0 fixes the pymongo 4.9+ compatibility.
motor>=3.6.0,<4.0.0
pymongo>=4.7.0,<5.0.0
pydantic-mongo==2.3.0

# ── LangChain ────────────────────────────────────────────────────────
langchain==0.1.20
langchain-text-splitters>=0.0.1

# ── LLM Providers ───────────────────────────────────────────────────
openai>=1.40.0,<2.0.0
cohere==5.5.8
httpx>=0.24.0,<1.0.0

# ── Core pinned (conflict prevention) ───────────────────────────────
numpy>=1.24.0,<2.0.0
tokenizers>=0.19.0,<0.30.0

# ── Embeddings & Vector Search ───────────────────────────────────────
sentence-transformers>=2.7.0,<4.0.0
chromadb>=0.5.0,<1.0.0
qdrant-client>=1.9.0,<2.0.0
tiktoken>=0.7.0

# ── PDF Extraction ──────────────────────────────────────────────────
pymupdf==1.24.3
docling>=2.90.0
docling-core>=2.0.0
pillow>=10.0.0

# ── OCR Engines ─────────────────────────────────────────────────────
paddlepaddle>=2.6.0
paddleocr>=2.8.0
easyocr>=1.7.0

# ── Vision/LLM (Ollama) ─────────────────────────────────────────────
ollama>=0.3.0